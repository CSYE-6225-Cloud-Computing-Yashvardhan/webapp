name: Webapp test

on:
    pull_request:
      branches:
        - main

jobs:
  build-and-test:
    name: "Build and Test"

    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4